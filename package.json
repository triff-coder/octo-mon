{
  "name": "octo-mon",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Scriptable iPhone widget showing Octopus Energy Agile usage cost, powered by an Octopus Home Mini",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^5.20250906.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.0",
    "wrangler": "^4.35.0"
  }
}
